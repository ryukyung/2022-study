// 상수와 변수
import Swift

let constant: String = "차후에 변경이 불가능한 상수 let"
var variable: String = "차후에 변경이 가능한 변수 var"
variable = "변수는 이렇게 차후에 다른 값을 할당할 수 있지만"
// constant = "상수는 여기서 오류가 발생한다"
let sum: Int
let inputA: Int = 100
let inputB: Int = 200
sum = inputA+inputB //300
print(sum) //300
var Name: String
Name = "yagom"
print(Name) //yagom
Name = "야곰" // 변수(var로 선언)는 변경 가능
print(Name) //야곰
